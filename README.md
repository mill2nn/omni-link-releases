# Omni Link — releases

Built files for the [Omni Link](https://github.com/mill2nn) Premiere Pro panel.
The panel reads `latest.json` to see whether a newer version exists.

This repo holds **only built output** — no source history, no tests.

Current version: **1.2.0**
